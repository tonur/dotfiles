---
- defaults:
    link:
      relink: true

- clean: [ '~' ]

- link:
    ~/:
      glob: true
      path: ./.**
      create: true
      exclude: [ .git ]

    ~/.config:
      glob: true
      path: ./.config/**
      create: true
