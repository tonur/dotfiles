---
- defaults:
    link:
      relink: true

- clean: [ '~' ]
  force: true

- link:
    ~/:
      glob: true
      path: ./.**
      exclude: [ .git ]

    ~/config/:
      glob: true
      path: ./.config/**
